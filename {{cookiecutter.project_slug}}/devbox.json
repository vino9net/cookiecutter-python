{
  "packages": [
    "python@3.11",
    "poetry@1.6.1"{%- if "pandas" in cookiecutter.extra_packages -%},
    "gcc-unwrapped.lib",
    "python311Packages.pandas@2.0.3",
    "zlib@latest"{%- endif %}
  ],
  "shell": {
    "init_hook": [
      "poetry install"
    ],
    "scripts": {
      "test": "poetry run pytest -s -v"
    }
  }
}